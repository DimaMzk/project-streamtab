{
  "name": "server_client",
  "version": "1.0.0",
  "main": "build/index.js",
  "license": "MIT",
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "electron": "^21.2.2",
    "typescript": "^4.8.4"
  },
  "scripts": {
    "start": "yarn build && electron ./build",
    "build": "tsc && copyfiles -f  src/index.html build"
  }
}
